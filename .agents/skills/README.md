# Agent skills

The skills in this folder are written by Matt Pocock and used here under the
MIT License (see [`LICENSE`](LICENSE)). Each `SKILL.md` names its author and
license in its frontmatter.

They are agent configuration, not application source, so the Apache-2.0 terms
in the repository's `LICENSE` and `NOTICE` do not cover them. How this
repository wires them up is in [`docs/agents/`](../../docs/agents/).
