[**fderuiter-portfolio**](../../../../README.md)

***

[fderuiter-portfolio](../../../../modules.md) / [lib/crf/form-test-harness](../README.md) / buildScopedKey

# Function: buildScopedKey()

> **buildScopedKey**(`scope`, `fieldId`): `string`

Builds the storage key for one field within one scope. This mirrors the key
shape the EDC simulator already uses, so values captured in the dock remain
legible to the same tooling.

## Parameters

### scope

[`FormTestScope`](../interfaces/FormTestScope.md)

### fieldId

`string`

## Returns

`string`
