[**fderuiter-portfolio**](../../../../../README.md)

***

[fderuiter-portfolio](../../../../../modules.md) / [lib/security-scan/internal/catalog](../README.md) / SECRET\_DETECTORS

# Variable: SECRET\_DETECTORS

> `const` **SECRET\_DETECTORS**: readonly \[\{ `appliesTo`: readonly \[`"contentGuard"`, `"historyAudit"`\]; `description`: `"Database or cache connection URL with an embedded username and password (Postgres/Neon, MySQL, MongoDB incl. mongodb+srv, Redis/Upstash rediss)"`; `gitPattern`: "(postgres\|postgresql\|mongodb(\\+srv)?\|redis\|rediss\|mysql)://\[^\[:space:\]/:\]+:\[^\[:space:\]@\]+@"; `id`: `"database-url-credentialed"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`\]; `description`: `"Database or cache connection URL scheme/host without embedded credentials"`; `gitPattern`: "(postgres\|postgresql\|mongodb(\\+srv)?\|redis\|rediss\|mysql)://\[^\[:space:\]/\]+"; `id`: `"database-url-bare"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`\]; `description`: `"Generic API Key/Secret/Password"`; `gitPattern`: "(api\[\_-\]?key\|secret\[\_-\]?key\|private\[\_-\]?key\|password\|auth\[\_-\]?token\|access\[\_-\]?token\|session\[\_-\]?token)\[\[:space:\]\]\*\[:=\]\[\[:space:\]\]\*\['\"\`\]\[a-zA-Z0-9\_.-\]\{16,\}\['\"\`\]"; `id`: `"generic-assigned-secret"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`, `"historyAudit"`\]; `description`: `"AWS access key ID (long-term AKIA or temporary/session ASIA)"`; `gitPattern`: "(AKIA\|ASIA)\[0-9A-Z\]\{16\}"; `id`: `"aws-access-key"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`, `"historyAudit"`\]; `description`: `"GitHub token (classic ghp_, fine-grained github_pat_, or app-issued gho_/ghu_/ghs_/ghr_)"`; `gitPattern`: "(github\_pat\_\[A-Za-z0-9\_\]\{20,\}\|gh\[oprsu\]\_\[A-Za-z0-9\]\{20,\})"; `id`: `"github-token"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`, `"historyAudit"`\]; `description`: `"PEM-encoded private key block"`; `gitPattern`: `"BEGIN [A-Z ]+ PRIVATE KEY"`; `id`: `"private-key-pem"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`, `"historyAudit"`\]; `description`: `"Stripe or Clerk secret key (sk_live_/sk_test_ — both providers share this prefix shape)"`; `gitPattern`: "sk\_(live\|test)\_\[A-Za-z0-9\_\]\{16,\}"; `id`: `"stripe-or-clerk-secret-key"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`, `"historyAudit"`\]; `description`: `"OpenAI project-scoped API key"`; `gitPattern`: `"sk-proj-[A-Za-z0-9_-]{16,}"`; `id`: `"openai-project-key"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`, `"historyAudit"`\]; `description`: `"Resend API key"`; `gitPattern`: `"re_[A-Za-z0-9_]{20,}"`; `id`: `"resend-api-key"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`, `"historyAudit"`\]; `description`: `"Vercel platform token (CLI/API access token or Blob read-write token)"`; `gitPattern`: "(vcp\_\[A-Za-z0-9\_\]\{20,\}\|vercel\_blob\_rw\_\[A-Za-z0-9\_\]\{20,\})"; `id`: `"vercel-token"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`, `"historyAudit"`\]; `description`: `"npm registry access token"`; `gitPattern`: `"npm_[A-Za-z0-9]{20,}"`; `id`: `"npm-token"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`, `"historyAudit"`\]; `description`: `"GitLab personal access token"`; `gitPattern`: `"glpat-[A-Za-z0-9_-]{20,}"`; `id`: `"gitlab-token"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`, `"historyAudit"`\]; `description`: `"Slack bot/user/app/refresh token"`; `gitPattern`: `"xox[baprs]-[A-Za-z0-9-]{20,}"`; `id`: `"slack-token"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`, `"historyAudit"`\]; `description`: `"Sentry internal-integration or user auth token"`; `gitPattern`: `"sntry[us]_[A-Za-z0-9_]{10,}"`; `id`: `"sentry-auth-token"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`\]; `description`: `"Upstash REST API token assigned to an UPSTASH_*TOKEN-named variable"`; `gitPattern`: "UPSTASH\_\[A-Z0-9\_\]\*TOKEN\[\[:space:\]\]\*\[:=\]\[\[:space:\]\]\*(\['\"\`\]\[A-Za-z0-9\_-\]\{16,\}\['\"\`\]\|\[A-Za-z0-9\_-\]\{16,\})"; `id`: `"upstash-rest-token"`; `regex`: `RegExp`; \}, \{ `appliesTo`: readonly \[`"contentGuard"`, `"historyAudit"`\]; `description`: `"Neon Postgres role password (npg_ prefix), on its own rather than inside a connection URL"`; `gitPattern`: `"npg_[A-Za-z0-9]{12,}"`; `id`: `"neon-role-password"`; `regex`: `RegExp`; \}\]

The single source of truth for every secret detector in the repository.

Each entry owns its identifier, human description, live-text regex, and a
POSIX-extended-regex `gitPattern` usable with `git log -G<pattern>` for
history/pickaxe scanning. `appliesTo` declares which scanning surfaces
(`ScannerSurface`, see `types.ts`) the detector is confident enough to
run under.

Order matters for `contentGuard`-surface scanning: `scanText` reports the
first detector that matches a given line, so detectors most likely to
co-occur with a broader/noisier detector on the same line are listed
first. New detectors are appended to preserve that ordering.
