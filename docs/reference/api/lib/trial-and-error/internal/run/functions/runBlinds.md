[**fderuiter-portfolio**](../../../../../README.md)

***

[fderuiter-portfolio](../../../../../modules.md) / [lib/trial-and-error/internal/run](../README.md) / runBlinds

# Function: runBlinds()

> **runBlinds**(`act`, `run`): `object`[]

The Blinds this run plays, in order. An act with a boss pool contributes
its Small and Big Blinds and the Boss the run drew.

## Parameters

### act

#### blinds

`object`[] = `...`

#### bossPool?

`object`[] = `...`

#### crisisDeck?

`object`[] = `...`

#### id

`string` = `identifier`

#### title

`string` = `...`

### run

`Pick`\<[`RunState`](../interfaces/RunState.md), `"bossId"`\>

## Returns
